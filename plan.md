# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-97f: defender/frontend/game/hud.js
- Scope: Implement `defender/frontend/game/hud.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/hud.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-97f` is closed only after verification passes.

### te-35c: defender/frontend/game/main.js
- Scope: Implement `defender/frontend/game/main.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/main.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-35c` is closed only after verification passes.

### te-0wq: defender/frontend/game/renderer.js
- Scope: Implement `defender/frontend/game/renderer.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/renderer.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-0wq` is closed only after verification passes.

### te-6r2: defender/frontend/index.html
- Scope: Implement `defender/frontend/index.html` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/index.html` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-6r2` is closed only after verification passes.

### te-ded: defender/frontend/style.css
- Scope: Implement `defender/frontend/style.css` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/style.css` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-ded` is closed only after verification passes.

## Phase context

Active delivery phase `frontend-structure` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

