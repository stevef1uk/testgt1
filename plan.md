# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-nrv: defender/backend/requirements.txt
- Scope: Implement `defender/backend/requirements.txt` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/backend/requirements.txt` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-nrv` is closed only after verification passes.

### te-skr: defender/backend/main.py
- Scope: Implement `defender/backend/main.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/backend/main.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-skr` is closed only after verification passes.

### te-qm5: defender/backend/tests/test_api.py
- Scope: Implement `defender/backend/tests/test_api.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `defender/backend/tests/test_api.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-qm5` is closed only after verification passes.

### te-m48: defender/backend/tests/test_score.py
- Scope: Implement `defender/backend/tests/test_score.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `defender/backend/tests/test_score.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-m48` is closed only after verification passes.

## Phase context

Active delivery phase `backend-setup` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

