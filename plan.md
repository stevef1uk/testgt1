# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-dlw: defender/backend/requirements.txt
- Scope: Implement `defender/backend/requirements.txt` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/backend/requirements.txt` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-dlw` is closed only after verification passes.

### te-od6: defender/README.md
- Scope: Implement `defender/README.md` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/README.md` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-od6` is closed only after verification passes.

### te-pc5: defender/backend/main.py
- Scope: Implement `defender/backend/main.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/backend/main.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-pc5` is closed only after verification passes.

## Phase context

Active delivery phase `backend-setup` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

