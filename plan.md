# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-gvd: backend/requirements.txt
- Scope: Implement `backend/requirements.txt` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `backend/requirements.txt` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-gvd` is closed only after verification passes.

### te-gvd: defender/requirements.txt
- Scope: Implement `defender/requirements.txt` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/requirements.txt` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-gvd` is closed only after verification passes.

### te-8pk: backend/main.py
- Scope: Implement `backend/main.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `backend/main.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-8pk` is closed only after verification passes.

### te-prd: backend/tests/test_api.py
- Scope: Implement `backend/tests/test_api.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `backend/tests/test_api.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-prd` is closed only after verification passes.

### te-pk8: backend/tests/test_score.py
- Scope: Implement `backend/tests/test_score.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `backend/tests/test_score.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-pk8` is closed only after verification passes.

### te-l8s: defender/
- Scope: Implement `defender/` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-l8s` is closed only after verification passes.

### te-9a4: defender/README.md
- Scope: Implement `defender/README.md` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/README.md` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-9a4` is closed only after verification passes.

### te-8pk: defender/main.py
- Scope: Implement `defender/main.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/main.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-8pk` is closed only after verification passes.

### te-prd: defender/test_api.py
- Scope: Implement `defender/test_api.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `defender/test_api.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-prd` is closed only after verification passes.

### te-pk8: defender/test_score.py
- Scope: Implement `defender/test_score.py` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `defender/test_score.py` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-pk8` is closed only after verification passes.

### te-cfe: frontend/game/bullets.js
- Scope: Implement `frontend/game/bullets.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/bullets.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-cfe` is closed only after verification passes.

### te-2iz: frontend/game/enemies.js
- Scope: Implement `frontend/game/enemies.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/enemies.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-2iz` is closed only after verification passes.

### te-0t5: frontend/game/hud.js
- Scope: Implement `frontend/game/hud.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/hud.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-0t5` is closed only after verification passes.

### te-lrl: frontend/game/humanoids.js
- Scope: Implement `frontend/game/humanoids.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/humanoids.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-lrl` is closed only after verification passes.

### te-zu9: frontend/game/input.js
- Scope: Implement `frontend/game/input.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/input.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-zu9` is closed only after verification passes.

### te-0nr: frontend/game/main.js
- Scope: Implement `frontend/game/main.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/main.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-0nr` is closed only after verification passes.

### te-y5i: frontend/game/player.js
- Scope: Implement `frontend/game/player.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/player.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-y5i` is closed only after verification passes.

### te-fyb: frontend/game/renderer.js
- Scope: Implement `frontend/game/renderer.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/renderer.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-fyb` is closed only after verification passes.

### te-2yc: frontend/game/world.js
- Scope: Implement `frontend/game/world.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/game/world.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-2yc` is closed only after verification passes.

### te-i98: frontend/index.html
- Scope: Implement `frontend/index.html` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/index.html` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-i98` is closed only after verification passes.

### te-wka: frontend/style.css
- Scope: Implement `frontend/style.css` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `frontend/style.css` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-wka` is closed only after verification passes.

