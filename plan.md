# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-4i9: defender/frontend/game/bullets.js
- Scope: Implement `defender/frontend/game/bullets.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/bullets.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-4i9` is closed only after verification passes.

### te-6eb: defender/frontend/game/enemies.js
- Scope: Implement `defender/frontend/game/enemies.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/enemies.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-6eb` is closed only after verification passes.

### te-kxj: defender/frontend/game/hud.js
- Scope: Implement `defender/frontend/game/hud.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/hud.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-kxj` is closed only after verification passes.

### te-i7o: defender/frontend/game/humanoids.js
- Scope: Implement `defender/frontend/game/humanoids.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/humanoids.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-i7o` is closed only after verification passes.

### te-6y8: defender/frontend/game/input.js
- Scope: Implement `defender/frontend/game/input.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/input.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-6y8` is closed only after verification passes.

### te-04l: defender/frontend/game/main.js
- Scope: Implement `defender/frontend/game/main.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/main.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-04l` is closed only after verification passes.

### te-4ex: defender/frontend/game/player.js
- Scope: Implement `defender/frontend/game/player.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/player.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-4ex` is closed only after verification passes.

### te-2xy: defender/frontend/game/renderer.js
- Scope: Implement `defender/frontend/game/renderer.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/renderer.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-2xy` is closed only after verification passes.

### te-ix9: defender/frontend/game/world.js
- Scope: Implement `defender/frontend/game/world.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `defender/frontend/game/world.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-ix9` is closed only after verification passes.

## Phase context

Active delivery phase `frontend-game` only. Later phases start automatically after QA `all_passed` (or use `gt rig set-phase` to switch manually).

